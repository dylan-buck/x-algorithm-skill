---
const antiPatterns = [
  {
    name: "The Engagement Farmer",
    behaviors: ["Explicit calls to like/RT/follow", "\"Engagement pods\" or coordinated liking", "Growth hacking tactics"],
    why: "These signals are learned as inauthentic. The model predicts \"not interested\" responses.",
    triggers: "Not Interested",
  },
  {
    name: "The Reply Guy",
    behaviors: ["Commenting on every viral post", "Self-promotional replies", "First-to-respond gaming"],
    why: "Low-quality engagement patterns lead to mutes.",
    triggers: "Mute",
  },
  {
    name: "The Thread Maximalist",
    behaviors: ["Every thought becomes a 20-tweet thread", "Padding threads with unnecessary posts", "\"1/\" on content that doesn't need threading"],
    why: "Author diversity penalty kicks in. Later posts in thread get discounted.",
    triggers: "Algorithm Penalty",
  },
  {
    name: "The Controversy Farmer",
    behaviors: ["Intentionally inflammatory takes", "Punching down for engagement", "Rage bait framing"],
    why: "Short-term replies, long-term blocks and mutes.",
    triggers: "Block, Mute",
  },
  {
    name: "The Copy-Paste Recycler",
    behaviors: ["Same content reposted weekly", "Slight variations of the same post", "Cross-posting without adaptation"],
    why: "Deduplication and previously-seen filters catch this.",
    triggers: "Filtered Out",
  },
];

const checklistItems = [
  "Am I explicitly asking for engagement? (remove it)",
  "Would I be annoyed seeing this from someone else? (reconsider)",
  "Is this the 5th+ post today? (wait)",
  "Am I being hostile to anyone? (reframe)",
  "Have I posted something similar recently? (don't)",
  "Does this deliver on its hook? (must)",
  "Would someone unfollow over this? (pause)",
];
---

<section id="anti-patterns" class="border-t border-zinc-200">
  <div class="mx-auto max-w-5xl px-6 py-16">
    <h2 class="text-2xl font-semibold text-zinc-900 text-balance">Anti-Patterns: What Gets Penalized</h2>
    <p class="mt-4 text-zinc-600 text-pretty">
      The algorithm has explicit negative signals that reduce your score. Understanding what triggers them is as important as understanding what drives positive engagement.
    </p>

    <div class="mt-12 space-y-6">
      {antiPatterns.map((pattern) => (
        <div class="rounded-lg border border-zinc-200 bg-white p-6">
          <div class="flex items-start justify-between gap-4">
            <div>
              <h3 class="font-medium text-zinc-900">{pattern.name}</h3>
              <span class="mt-1 inline-flex rounded-full bg-red-100 px-2 py-0.5 text-xs font-medium text-red-700">
                Triggers: {pattern.triggers}
              </span>
            </div>
          </div>
          <ul class="mt-4 space-y-1 text-sm text-zinc-600">
            {pattern.behaviors.map((behavior) => (
              <li class="flex items-start gap-2">
                <span class="mt-1 h-1.5 w-1.5 flex-shrink-0 rounded-full bg-red-400"></span>
                <span>{behavior}</span>
              </li>
            ))}
          </ul>
          <p class="mt-4 text-sm text-zinc-500 text-pretty">
            <strong>Why suppressed:</strong> {pattern.why}
          </p>
        </div>
      ))}
    </div>

    <!-- Red Flags Checklist -->
    <div class="mt-12 rounded-lg border border-zinc-200 bg-zinc-50 p-6">
      <h3 class="text-lg font-medium text-zinc-900">Red Flags Checklist</h3>
      <p class="mt-2 text-sm text-zinc-600">Before posting, check:</p>
      <ul class="mt-4 space-y-3">
        {checklistItems.map((item) => (
          <li class="flex items-center gap-3 text-sm text-zinc-700">
            <input type="checkbox" class="h-4 w-4 rounded border-zinc-300 text-zinc-900" disabled />
            <span>{item}</span>
          </li>
        ))}
      </ul>
    </div>
  </div>
</section>
